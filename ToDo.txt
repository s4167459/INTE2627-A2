Part 1 - Task 1:
- Initialise the cryptographic parameters required for digital signature operations using the values
provided in the List of Keys document. --DONE
- Derive any additional key components required for the digital signature process from the provided
parameters, and ensure all required values are explicitly defined in your code. --DONE--
- Implement a mechanism that enables an inventory node to digitally sign a newly generated inventory
record prior to submission. --DONE--
- Implement a verification process that allows other inventory nodes to validate the authenticity and
integrity of the received record before it proceeds to the consensus stage. --DONE--

Part 1 - Task 2:
- Select and justify an appropriate consensus mechanism for the given scenario, explaining how it
ensures global agreement and handles inconsistent or malicious updates, including key trade-offs.
- Implement the selected consensus mechanism (in a simplified form) to determine whether a newly
submitted record should be accepted or rejected.
- Ensure that all inventory nodes reach a consistent decision before the record is stored locally.
- After a successful consensus outcome, store the accepted record in each inventory node’s local
database.

Part 2 - Task 3:

- ENSURE ALL WORK IS CLEANED UP

All tasks:
- separate CSV files for all warehouse inventory systems, makes life easier.



Report:
- For task 1, in your report, explain how digital signatures contribute to secure record submission in a distributed
inventory environment.
- For task 3, in your report, explain how multi-signature verification and secure delivery mechanisms improve trust,
accountability, and confidentiality in distributed inventory queries.